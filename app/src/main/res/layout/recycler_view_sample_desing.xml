<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cl_vehicle_sample_design"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white">

    <TextView
        android:id="@+id/tv_vehicle_vehicle_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="2dp"
        android:padding="2dp"
        android:text="BUS1"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />

    <View
        android:id="@+id/v_vehicle_status_color"
        android:layout_width="12dp"
        android:layout_height="10dp"
        android:layout_marginEnd="4dp"
        android:background="#FF5722"
        app:layout_constraintBottom_toBottomOf="@+id/tv_vehicle_vehicle_name"
        app:layout_constraintEnd_toStartOf="@+id/tv_vehicle_vehicle_name"
        app:layout_constraintTop_toTopOf="@+id/tv_vehicle_vehicle_name" />

    <TextView
        android:id="@+id/tv_vehicle_vehicle_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:text="Female"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@+id/tv_vehicle_vehicle_name"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tv_vehicle_vehicle_name" />

    <TextView
        android:id="@+id/tv_vehicle_road"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Maril -> Sahar -> Gasspara"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="@+id/tv_vehicle_vehicle_type"
        app:layout_constraintStart_toStartOf="@+id/tv_vehicle_vehicle_name"
        app:layout_constraintTop_toBottomOf="@+id/tv_vehicle_vehicle_name" />

    <View
        android:id="@+id/view2"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:layout_marginTop="4dp"
        android:background="#000000"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_vehicle_road" />

</androidx.constraintlayout.widget.ConstraintLayout>